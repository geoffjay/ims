configure_file(
          input: 'ims.service.in',
         output: 'ims.service',
  configuration: subst,
        install: true,
    install_dir: join_paths(get_option('sysconfdir'), 'systemd', 'system'),
)
